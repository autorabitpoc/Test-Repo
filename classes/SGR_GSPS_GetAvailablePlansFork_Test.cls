@isTest
public class SGR_GSPS_GetAvailablePlansFork_Test {
    
      static testMethod void testMethod1(){
        
        
      /*  Division__c div=new Division__c();
        div.name='IL';
        insert div;
        
        sgr_product_group__c pd=new sgr_product_group__c();
        pd.name='Dental';
        pd.SGR_Product_Group_Code__c='QHP_NM_F34';
        pd.SGR_Product_Group_Type__c ='Dental';
        pd.SGR_Market_Segment__c='SG';
        pd.SGR_Funding_Type__c='PREM';
        pd.SGR_Effective_Date__c=System.today();
        insert pd;
          
        Integration_Log_Switcher__c intSwitch=new Integration_Log_Switcher__c();
        intSwitch.Integration_Log__c=true;
        intSwitch.name='GSPS All Plan Service';
        insert intSwitch;
          
        list<sgr_product_group__c> pdsLst=[select id from sgr_product_group__c where SGR_Product_Group_Code__c=:'QHP_NM_F34'];
        set<id> prods=new set<id>();
          for(sgr_product_group__c p:pdsLst){
              prods.add(p.id);
          }
        
          
        test.startTest();
        SGR_GSPS_GetAvailablePlansForkedBatch sb = new SGR_GSPS_GetAvailablePlansForkedBatch(prods);
        DataBase.executeBatch(sb);
        test.stopTest();
        */
    }
   
}